namespace ConsoleApp2;

public class FileParser_: IFileParser
{
    public void Parse(string pathToFile)
    {
        
        var file=new File (pathToFile)
        
        var reader = new StreamReader(pathToFile);
        reader.
    }
}